# MVP Schema Implementation Plan

## Table of Contents

- [Purpose](#purpose)
- [Single Source of Truth Strategy](#single-source-of-truth-strategy)
- [Scope (MVP)](#scope-mvp)
- [Implementation Approach (EF Core + PostgreSQL)](#implementation-approach-ef-core--postgresql)
- [Migration Waves (Planned Order)](#migration-waves-planned-order)
- [Schema Conventions](#schema-conventions)
- [Seed Data Strategy](#seed-data-strategy)
- [Testing Strategy](#testing-strategy)
- [Execution Workflow](#execution-workflow)
- [Next Implementation Tasks](#next-implementation-tasks)

## Purpose

This document defines the backend implementation plan for the MVP relational data model.

It is intentionally focused on:

- schema boundaries and relationships
- migration order
- EF Core implementation conventions
- seed and testing strategy

It is **not** intended to duplicate every column/constraint from EF Core migrations once implementation begins.

## Single Source of Truth Strategy

To minimize duplication and drift, the project uses a single source of truth per layer:

- API contract SSOT: OpenAPI definition in the `api` submodule (`api/postman/specs/...`)
- Database schema SSOT: EF Core entity configurations + EF Core migrations in `backend/`
- Domain behavior SSOT: backend code and automated tests

Implications:

- Use docs for decisions/rationale and migration sequencing.
- Use EF Core migrations for exact table/column/index/constraint definitions.
- Avoid maintaining a second exhaustive schema document after migrations exist.

## Scope (MVP)

Included now:

- unified `users` model (developers and players)
- platform roles (`player`, `developer`, `admin`, `moderator`) via many-to-many user roles
- email/password and OAuth-ready identity tables
- optional Board profile linkage (reference/cache only; Board remains source of truth)
- organizations and memberships
- titles and versioned metadata
- media asset slots (`card`, `hero`, `logo`)
- releases and APK artifacts
- external integration connections/bindings (content-host oriented)

Deferred:

- payments / checkout / orders / entitlements
- full taxonomy normalization (genre taxonomy, etc.)
- advanced moderation workflows
- Board profile/device deep integration (pending SDK/API capability)

## Implementation Approach (EF Core + PostgreSQL)

### Database SSOT

The PostgreSQL schema will be created and evolved entirely from EF Core migrations.

Recommended structure (MVP can stay in API project initially):

- `DbContext` implementation (single app context)
- entity classes (domain/persistence model)
- `IEntityTypeConfiguration<T>` per entity
- migration files (generated by EF Core)

### Postgres-specific features to use

- `uuid` keys for entity identity
- `jsonb` for provider-specific integration configuration
- `timestamptz` for audit timestamps
- explicit indexes, unique constraints, foreign keys
- check constraints for invariant enforcement
- table/column comments via EF Core/Npgsql (`HasComment`) for in-db documentation

### Schema namespace

Use PostgreSQL `public` schema for MVP to reduce complexity.

If bounded contexts later justify separation (`auth`, `catalog`, etc.), namespace split can be introduced in a later migration.

## Migration Waves (Planned Order)

The order below is designed to reduce circular FK complexity and allow small, reviewable migrations.

### Wave 1: Identity Core + Platform Roles

Tables:

- `users`
- `roles`
- `user_roles`
- `user_email_addresses`
- `user_password_credentials`
- `user_external_identities`
- `user_board_profiles`

Key notes:

- `users` is the stable identity root (`users.id` is the real identity key).
- Email is mutable and modeled separately in `user_email_addresses`.
- `user_password_credentials` supports local auth without forcing password fields onto `users`.
- `user_external_identities` supports OAuth provider links (`github`, `steam`, `epic`, etc.).
- `user_board_profiles` is optional/non-authoritative.

### Wave 2: Organizations + Memberships

Tables:

- `organizations`
- `organization_memberships`

Key notes:

- `organization_memberships.role` stays as a simple scoped role field (`owner`, `admin`, `editor`) for MVP.
- Global platform roles remain in `roles` / `user_roles`.

### Wave 3: Titles + Versioned Metadata

Tables:

- `titles`
- `title_metadata_versions`

Follow-up FK update in same or next migration:

- `titles.current_metadata_version_id -> title_metadata_versions.id`

Key notes:

- `titles` stores stable identity + ownership + publication status.
- `title_metadata_versions` stores versioned player-facing metadata snapshots.
- Keep `genre_display` for MVP; defer normalized genre taxonomy.

### Wave 4: Media + Releases + Artifacts

Tables:

- `title_media_assets`
- `title_releases`
- `release_artifacts`

Key notes:

- `title_media_assets.media_role` supports Board-style slots (`card`, `hero`, `logo`).
- `title_releases` can reference a metadata snapshot (`metadata_version_id`) to preserve historical display data alignment.
- `release_artifacts` starts with `apk` only, but modeled to expand later.

### Wave 5: External Integrations (Content Hosting)

Tables:

- `integration_connections`
- `title_integration_bindings`

Key notes:

- Focus on content-host integration roles in MVP (free APK links / external hosting).
- Keep provider-specific settings in `jsonb`.
- Defer payment provider roles until commerce is in scope.

## Schema Conventions

### Keys and references

- All primary keys are `UUID`
- All foreign keys reference `UUID` PKs
- Use explicit FK constraint names in EF configurations for readability

### Timestamps

Use `timestamptz` for all timestamps:

- `created_at` on all tables
- `updated_at` on mutable tables
- `published_at`, `verified_at`, `last_login_at`, etc. as needed

Application code should write UTC timestamps consistently.

### Status and role values

Prefer string `code`/`status` fields over DB enums for MVP:

- easier migrations during early iteration
- simpler seed updates
- fewer enum synchronization issues across app/API/DB

Use check constraints where the allowed set is stable enough for MVP.

### Email model (mutable by design)

`users` does not own a single immutable email field.

Instead:

- `user_email_addresses` stores one-to-many email records
- one active primary email per user
- email changes are implemented by adding/verifying/switching primary email rows

This prevents cascading changes to other tables and supports a low-friction user experience.

### JSON configuration

Use `jsonb` for:

- `integration_connections.config_json`
- `title_integration_bindings.config_json`

Only add GIN indexes after a real query pattern requires them.

### Comments / self-documenting DB

Use EF Core/Npgsql comments on:

- tables
- columns with non-obvious semantics
- provider-specific config JSON columns

This provides in-database documentation without duplicating schema docs.

## Seed Data Strategy

Seed via EF Core migrations (not runtime ad hoc scripts) for system-owned static data.

Initial seed set:

- `roles`
  - `player`
  - `developer`
  - `admin`
  - `moderator`

Seed guidance:

- Use stable UUIDs for seeded role IDs (hardcoded constants in migration/config)
- Keep role `code` values immutable once published
- Do not seed user accounts in migrations

## Testing Strategy

### Schema / migration tests (integration)

Add backend integration coverage for persistence once EF Core is introduced:

- database can apply all migrations to a fresh Postgres instance
- seed roles exist after migration
- key unique constraints behave as expected (example: duplicate role code rejected)

This can extend the existing Docker/Testcontainers integration test setup.

### API contract alignment

Keep using Postman contract tests for client-facing behavior.

The schema does not need a duplicate column-by-column doc as long as:

- EF migrations are versioned and reviewed
- Postman/OpenAPI contracts reflect externally observable data shapes

## Execution Workflow

Recommended developer workflow (schema changes):

1. Update/confirm API contract shape (if externally visible fields change)
2. Add/modify EF Core entities and configurations
3. Generate migration
4. Review migration SQL/operations for naming and constraints
5. Apply migration locally
6. Run backend tests (unit + integration)
7. Run Postman contract tests (mock and/or local backend as applicable)

## Next Implementation Tasks

Planned next backend work items (code-first):

1. Add EF Core + Npgsql EF provider packages to the API project
2. Add application `DbContext` and persistence registration
3. Implement Wave 1 entity types + configurations
4. Generate initial migration (Wave 1)
5. Add seed roles in migration/config
6. Add integration test that applies migrations and verifies seeded roles
7. Implement Wave 2 and Wave 3 incrementally

This plan keeps database schema definition fully reproducible from code while minimizing documentation duplication.
